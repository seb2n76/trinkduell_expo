# Fallback für clientseitiges Routing.
#
# Gleiches Format bei Netlify und Cloudflare Pages. Greift NUR, wenn keine
# statische Datei passt — Assets und die von Expo erzeugten Routen-HTMLs
# (feed.html, login.html, …) werden also weiterhin direkt ausgeliefert.
#
# Ohne diese Zeile zeigt der Host bei einem unbekannten Pfad seine eigene
# 404-Seite. Mit ihr lädt die App und expo-router entscheidet selbst, was zu
# tun ist — inklusive des eigenen "nicht gefunden"-Screens.
/*    /index.html    200
